import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    domains: ['raw.githubusercontent.com', 'tayuh2o6fu1ffl9a.public.blob.vercel-storage.com'],
  },
};

export default nextConfig;
